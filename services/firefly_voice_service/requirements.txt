# Firefly Voice Service requirements (Python 3.10)
#
# IMPORTANT: install torch FIRST, in one of three builds (device=auto picks at
# runtime; see README "CPU / GPU"):
#
#   NVIDIA (verified: RTX 4060 Laptop, cu121):
#     pip install torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   AMD on Windows (DirectML, fp32 -- e.g. RX 7600S):
#     pip install torch-directml
#   CPU only:
#     pip install torch==2.5.1 torchaudio==2.5.1
#
# Then:
#   pip install -r requirements.txt
#
# WARNING: this service pins numpy<2 and must live in its OWN venv.
# Never share this environment with the Firefly host application.

numpy>=1.26.4,<2
transformers>=4.49.0,<4.50      # RVC loads HuBERT in transformers format (no fairseq)
faiss-cpu>=1.11
librosa>=0.10.2,<0.11
soundfile>=0.13
scipy>=1.13
einops>=0.8
praat-parselmouth>=0.4.5       # f0-method=pm fallback
PyYAML>=6
tqdm>=4

# Service layer
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
httpx>=0.27                    # also required by fastapi.testclient (contract tests)

# v1.1+ expression / playback layer
sounddevice>=0.5               # AudioQueue real playback
edge-tts>=7                    # cloud TTS (LGPL-3.0) for the speak pipeline

# Audio decoding (RVC infer/audio.py)
ffmpeg-python>=0.2
av>=15
