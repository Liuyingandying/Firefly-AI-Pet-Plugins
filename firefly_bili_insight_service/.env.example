# 复制为 .env 使用。.env 已被 .gitignore 排除——永远不要把真实 .env 提交到 Git。

# 服务数据目录（缓存/抽帧输出），相对服务目录
DATA_DIR=./data

# ---- B站登录态（可选；留空 = 纯匿名模式）----
# 匿名即可完成公开视频的 metadata / transcribe(本地ASR) / frame。
# 登录只改善两件事：metadata 中 has_subtitle 字幕标志的准确度、
# 以及访问需要登录才能观看的视频。
# 值从你自己已登录浏览器的开发者工具（Application → Cookies）复制，
# 只放在本机 .env 或环境变量中，绝不提交到 Git、绝不写进示例文件。
# BILIBILI_SESSDATA=
# BILIBILI_BILI_JCT=
# BILIBILI_BUVID3=
# BILIBILI_BUVID4=
# BILIBILI_DEDEUSERID=
